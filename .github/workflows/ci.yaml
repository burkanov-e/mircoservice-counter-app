name: Docker Build

on:
  # push:
  pull_request:
    types:
      - closed
    branches:
      - develop

jobs:
  # extract pr metadata
  extract-pr-metadata:
    runs-on: ubuntu-latest
    outputs:
      pr-merged-commit-sha: ${{ github.event.pull_request.merge_commit_sha }}
    steps:
      - name: echo pr-merged-commit-sha
        run: |
          echo "PR merged commit SHA: ${{ github.event.pull_request.merge_commit_sha }}"

  docker-image-build:
    # if: github.event.pull_request.merged == true
    needs: [extract-pr-metadata]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Build Docker image
        run: |
          docker build -t counter-app-docker/microservice-counter-app:${{ github.event.pull_request.merge_commit_sha }} .
          # docker tag counter-app-docker/microservice-counter-app:${{ github.event.pull_request.merge_commit_sha }} counter-app-docker/microservice-counter-app:latest
          # docker push counter-app-docker/microservice-counter-app:latest

  # Optional: Push to Docker registry
  # - name: Login to Docker Hub
  #   uses: docker/login-action@v2
  #   with:
  #     username: ${{ secrets.DOCKERHUB_USERNAME }}
  #     password: ${{ secrets.DOCKERHUB_TOKEN }}

  # - name: Push to Docker Hub
  #   uses: docker/build-push-action@v4
  #   with:
  #     context: .
  #     push: true
  #     tags: counter-app-docker/my-app:latest
